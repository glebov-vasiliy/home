export enum unitStateEnum {
  enabled = 'enabled',
  disabled = 'disabled',
  inProgress = 'inProgress',
}

export enum LoadingStateEnum {
  IDLE = 'IDLE',
  ERROR = 'ERROR',
  LOADING = 'LOADING',
  SUCCESS = 'SUCCESS',
}
