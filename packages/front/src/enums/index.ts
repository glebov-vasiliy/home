export enum unitStateEnum {
  enabled = 'enabled',
  disabled = 'disabled',
  inProgress = 'inProgress',
}

export enum RequestStateEnum {
  IDLE = 'IDLE',
  ERROR = 'ERROR',
  LOADING = 'LOADING',
  SUCCESS = 'SUCCESS',
}

export enum Pages {
  main = 'main',
  profile = 'profile',
}
